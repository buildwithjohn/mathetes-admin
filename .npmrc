# Next 15.0.x pins an RC of React 19 as a peer; the project uses React 19
# stable, which works fine together. Relax npm peer resolution so installs do
# not need --legacy-peer-deps on the command line.
legacy-peer-deps=true
